import { OcultamientoPipe } from './ocultamiento.pipe';

describe('OcultamientoPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultamientoPipe();
    expect(pipe).toBeTruthy();
  });
});
